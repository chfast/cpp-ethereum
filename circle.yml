version: 1
machine:
  xcode:
    version: 8.3

compile:
  override: |
    pwd
    ls -l
    mkdir -p build && cd build
    clang --version
    cmake --version
    make --version
    ninja --version
    homebrew install cmake ninja

