version: 1
machine:
  xcode:
    version: 9.0

dependencies:
  override:
    - brew install cmake ninja

test:
  override:
    - pwd
    - ls -l
    - mkdir -p build && cd build
    - clang --version
    - cmake --version
    - make --version
    - ninja --version
    - homebrew install cmake ninja

