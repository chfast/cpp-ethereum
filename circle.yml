version: 1
machine:
  xcode:
    version: 8.3

dependencies:
  override:
    - brew install cmake ninja

test:
  override:
    - pwd
    - ls -l
    - mkdir -p build && cd build
    - clang --version
    - cmake --version
    - make --version
    - ninja --version
    - cd build && cmake .. -G Ninja
    - cd build && ninja

